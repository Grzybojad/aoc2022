#include <iostream>
#include <string>
#include <vector>
#include "helper/input_parse.hpp"


std::string solve_dayX_1(std::string input_filename) {
    std::vector<std::string> lines = read_lines_string(input_filename);

    for (size_t i = 0; i < lines.size(); i++) {
        std::string line = lines[i];
    }

    return 0;
}

std::string solve_dayX_2(std::string input_filename) {
    std::vector<std::string> lines = read_lines_string(input_filename);

    for (size_t i = 0; i < lines.size(); i++) {
        std::string line = lines[i];
    }

    return 0;
}